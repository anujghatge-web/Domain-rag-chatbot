streamlit>=1.36
python-dotenv>=1.0
pypdf>=4.2
sentence-transformers>=3.0
faiss-cpu>=1.8
numpy>=1.26
requests>=2.31

# LLM providers — install whichever you plan to use.
groq>=0.9                 # Groq (needs GROQ_API_KEY)
google-generativeai>=0.7  # Gemini (needs GEMINI_API_KEY)
# Ollama needs no Python package — install the Ollama app instead:
# https://ollama.com
